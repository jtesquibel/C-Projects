#include <vector>
#include "msort.h"

/*
void mergeSort(std::vector<T>& myArray, Comparator comp)
{
	
	if (myArray.size() == 1)
	{
		return;
	}
	else
	{
		middleVec = (myArray.begin()+myArray.end())/2;
		std::vector<T> leftVec(myArray.begin(), middleVec);
		std::vector<T> rightVec(middleVec, myArray.end());

		mergeSort(leftVec, comp);
		mergeSort(rightVec, comp);
		helperMerge(myArray, leftVec, rightVec, comp);
	} 
}

std::vector<T> helperMerge(std::vector<T>& myArray, leftVec, rightVec, Comparator comp)
{
	
	std::vector<T> temp;
	for (unsigned int i = leftVec.begin(); i < rightVec.end(); i++)
	{
		if (leftVec.begin() < )
	} 
} */
